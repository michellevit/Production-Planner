# NOTE: React environment variables must start with REACT_APP_ to be recognized in the build process
# NOTE: React will not automatically detect or use an .env file that is located outside of its root directory

REACT_APP_BACKEND_URL=http://XXX.XXX.X.XXX:8000 
# IF DEVELOPMENT MODE: REACT_APP_BACKEND_URL = http://localhost:8000
