build:
  docker:
    web: reactapp/Dockerfile
    worker: django/Dockerfile
    nginx: nginx/Dockerfile

run:
  web: cd reactapp && sh -c "npm install && npm start"
  worker: cd django && python manage.py runserver 0.0.0.0:8000
  nginx: cd nginx && <command-to-start-nginx>
